apiVersion: v1
kind: ConfigMap
metadata:
  name: pos-shared
data:
  NODE_ENV: production
  NATS_URL: nats://nats:4222
  NATS_MONITOR_URL: http://nats:8222
  LOG_LEVEL: info
  # Per-service DB URLs are secrets (they carry credentials); see secret.example.yaml.
