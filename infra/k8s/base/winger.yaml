# winger service — HTTP API (Deployment + Service + HPA + PDB). Behind Kong at
# /v1/businesses/{id}/winger-accounts/* and /v1/winger/*.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: winger
  labels:
    app.kubernetes.io/name: winger
    app.kubernetes.io/part-of: pos-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: winger
  template:
    metadata:
      labels:
        app.kubernetes.io/name: winger
    spec:
      containers:
        - name: winger
          image: ghcr.io/pos/winger:latest
          ports:
            - { name: http, containerPort: 3000 }
          envFrom:
            - configMapRef: { name: pos-shared }
            - secretRef: { name: pos-secrets }
          readinessProbe:
            httpGet: { path: /readyz, port: 3000 }
          livenessProbe:
            httpGet: { path: /healthz, port: 3000 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: winger
  labels:
    app.kubernetes.io/name: winger
spec:
  selector:
    app.kubernetes.io/name: winger
  ports:
    - { name: http, port: 3000, targetPort: 3000 }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: winger
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: winger
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: winger
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: winger
