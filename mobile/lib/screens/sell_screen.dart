import 'package:flutter/material.dart';
import 'stub_screen.dart';

class SellScreen extends StatelessWidget {
  const SellScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubScreen(title: 'Sell', task: 'Phase 04');
}
